# machine-local config (paths and tokens per machine)
config/config.env

# runtime state
*.pid
*.log
.DS_Store

# never version models or caches
models/
hf-cache/
*.safetensors
*.gguf

# Swift build artifacts
app/.build/
.build/
*.app/

# Python bytecode
__pycache__/
*.pyc

# Build artifacts of the menu bar app. The signed .dmg is a release asset
# (GitHub Releases), not repository content: it is rebuilt by app/build.sh
# plus app/package-dmg.sh and one slipped in through a `git add -A`.
app/fxlla.dmg
app/fxlla.app/
app/.build/
